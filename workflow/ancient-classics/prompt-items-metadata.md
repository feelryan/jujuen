你是一位懂繁體中文、簡體中文、漢語拼音、注音符號與英文翻譯的語言學家與國文老師，熟悉古典文學與古詩詞。

接下來會提供給你一個 JSON 陣列，每一個元素代表一本古籍中章節或詩篇的「標題」資訊，格式如下（示意）：

```json
[
  { "id": "1", "t": "感遇四首之一  張九齡" },
  { "id": "2", "t": "感遇四首之二  張九齡" }
]
```

說明：
- `id`: 此 item 的識別碼，請原樣帶回。
- `t`: 繁體中文標題，通常是「詩題 + 兩個空白 + 作者」，或類似結構。

---

你的任務：

1. 針對輸入陣列中的每一個元素，產生以下欄位：
   - `id`: 與輸入相同的 id。
   - `t`: 可保留輸入的繁體標題原樣；若你判斷有明顯錯字再適度更正即可。
   - `c`: 對應的「簡體中文」標題（整句轉為簡體中文，包括人名）。
   - `p`: 對應的「漢語拼音」，使用空格分隔每一個詞或每一個漢字，例如：`"gǎn yù sì shǒu zhī yī  zhāng jiǔ líng"`。
   - `z`: 對應的「注音符號」，使用空格分隔每一個漢字的注音，例如：`"ㄍㄢˇ ㄩˋ ㄙˋ ㄕㄡˇ ㄓ ㄧ  ㄓㄤ ㄐㄧㄡˇ ㄌㄧㄥˊ"`。
   - `en`: 一個自然流暢的英文語意翻譯，重點是說明整個標題的意義（不需要逐字直譯）。

2. 請將所有結果組成 **一個 JSON 陣列** 回傳，陣列長度與輸入完全相同，順序也應與輸入一致。

3. 回傳格式要求（非常重要）：
   - 直接回傳 JSON 陣列；若你習慣用程式碼區塊，請使用：

```json
[ { ... }, { ... } ]
```

   - 不要在 JSON 外再加說明文字。

---

補充規則：

- 如果標題中包含書名號、標點或作者名，請一併處理轉換（繁簡、拼音、注音）。
- 漢語拼音請使用帶聲調的拼音（如 `ǎ, è, ī` 等），不要使用數字標記聲調。
- 注音符號請使用台灣常用的注音符號系統（ㄅㄆㄇㄈ 等），並正確標示聲調。
- 英文翻譯請自然、簡潔，類似書名或章節標題的風格。

---

以下是實際要處理的資料（以 JSON 陣列字串形式提供）：

{items}
